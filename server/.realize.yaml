settings:
    legacy:
      force: false
      interval: 0s
schema:
- name: server
  path: ./
  commands:
    build:
      status: true
      method: go build -o /go/bin/SNS/server
    run:
      status: true
      method: /go/bin/SNS/server
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize